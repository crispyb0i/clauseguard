{
  "name": "clauseguard-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@sentry/node": "^8.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "dotenv": "^17.3.1"
  }
}
